Source: ai-agent-skills-playbook, docs/protocols/agent-metrics-v1.md + contracts/agent-metrics/v1/
Vendored commit: d99e48057a98af80871d00ace90f2ca18ae78eba
Vendored at: 2026-08-07
Vendored into: packages/core/test/fixtures/agent-metrics/v1/
  (envelope.schema.json, token-usage.schema.json, fixtures/*.json, fixtures/*.marker.txt —
  18 files total, byte-for-byte copies, no local modification)

Fixture tree hash (sha256 over `find . -type f | sort` each file's path+content,
reproducible via: `find packages/core/test/fixtures/agent-metrics/v1 -type f | sort |
xargs -I{} sh -c 'echo "{}"; cat "{}"' | shasum -a 256`):

  11b5050d9a95d7ee5f70817a703db27849578009d31bc5fc0988e27d2c4f9350

Hash correction (2026-08-20 pin-verify audit): the value recorded here was previously
5b05a2ce8f8bb83ad63f195d7491d520f6094a87a22ad9569508b1f2f832b0d7, which did NOT reproduce
from the command documented immediately above it -- that command hashes each file's path
relative to the repo root (e.g. `packages/core/test/fixtures/agent-metrics/v1/envelope.schema.json`),
but the recorded value had actually been computed by `cd`-ing into the fixture directory
first and hashing `find . -type f` output instead (dot-relative paths, e.g.
`./envelope.schema.json`). The vendored bytes themselves were never wrong -- re-diffed here
against upstream d99e48057a98af80871d00ace90f2ca18ae78eba's own contracts/agent-metrics/v1/
(minus the intentionally-excluded verify-fixtures.mjs) with `diff -rq`, 0 differences -- only
this marker's own recorded hash was self-inconsistent with its own documented reproduction
command. Corrected to the value the documented command actually produces, so this marker now
follows the same repo-root-relative-path convention as every other UPSTREAM marker in this
repo (needed for `lane`'s own pin-verify test suite,
packages/core/test/upstream-markers.test.ts, to treat all markers uniformly).

Re-vendoring: if the playbook's contract changes, re-copy the same files from that commit,
recompute the tree hash with the command above (run from the repo root, exactly as written --
see the correction note above), and update both the commit SHA and the hash in this file in
the same commit as the code changes that track the new contract version. **The pin MUST be a
commit that is an ancestor of upstream `main`.** This repo's own docs/design.md §4.5 records
the semantic summary of what this contract requires; this file's job is only to pin the exact
bytes tested against.
