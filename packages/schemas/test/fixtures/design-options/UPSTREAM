Source: ai-agent-skills-playbook, docs/protocols/design-options-v1.md + contracts/design-options/v1/

Vendored commit: 5e8884e9294bbac17ba88f21c03bec74f974d5fb (upstream `main`, merge of PR #7
"fix/independence-status-derivable")
Vendored at: 2026-08-19
Vendored into: packages/schemas/test/fixtures/design-options/v1/
  (design-options.schema.json, fixtures/*.json — byte-for-byte copies, no local
  modification)

Note on this pin's history: this contract was first vendored against
6b4f4233cf07e79040d0b8b65d7fe658d497112d, a commit on the (then-unmerged) branch
`fix/independence-status-derivable`, because a stale local `origin/main` remote-tracking
ref made it look like the engine_ref/critic/prior_involvement revision of
design-options/v1 did not exist on upstream `main` yet. `git fetch origin` in the
upstream checkout showed `main` had already moved past that (PR #7 merged as 5e8884e,
with 6b4f423 as its last commit) -- every vendored file's contents are byte-for-byte
identical at both commits (diffed individually, schema and all 23 fixtures), so only this
marker's recorded commit changed, not the vendored bytes or the tree hash below.

Fixture tree hash (sha256 over `find . -type f | sort` each file's path+content,
reproducible via: `find packages/schemas/test/fixtures/design-options/v1 -type f | sort |
xargs -I{} sh -c 'echo "{}"; cat "{}"' | shasum -a 256`):

  50ecceda65caf92bac93f7e72c719208f400bd86838b7c86bc1f48501fbf2846

Re-vendoring: if the playbook's contract changes, re-copy the same files from that commit,
recompute the tree hash with the command above, and update both the commit id and the
hash in this file in the same commit as the code changes that track the new contract
version. **The pin MUST be a commit that is an ancestor of upstream `main`** -- see this
marker's own "Note on this pin's history" above and
packages/adapters/test/fixtures/measure/UPSTREAM's 2026-08-20 re-pin history for two
different ways a pin can silently stop being that.
packages/schemas/src/design-options.ts (the hand-authored zod mirror) and
packages/schemas/test/design-options-fixtures.test.ts (the conformance-parity test that
replays this directory's own expected-results.json) record the semantic summary of what
this contract requires; this file's job is only to pin the exact bytes tested against,
same convention as packages/core/test/fixtures/{trace,attribution,estimate}/UPSTREAM and
packages/adapters/test/fixtures/measure/UPSTREAM.

Pin verification (R39/R40): see packages/core/src/vendor/derive-independence/UPSTREAM's
own "Pin verification" note -- the same commit (this contract and derive-independence.mjs
were vendored together, from the same upstream merge) and the same CI-mandatory/
end-user-opt-in split applies here.
