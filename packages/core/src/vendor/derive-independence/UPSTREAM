Source: ai-agent-skills-playbook, contracts/shared/derive-independence.mjs

Vendored commit: 7f3982de665d35f1ee0733ee5aea1bdbb66d6232 (upstream `main`, merge of PR #15
"feat/derive-independence-structured-reasons")
Vendored at: 2026-08-22
Vendored into: packages/core/src/vendor/derive-independence/v1/derive-independence.mjs
  (byte-for-byte copy, no local modification; verified identical to
  `git show 7f3982de665d35f1ee0733ee5aea1bdbb66d6232:contracts/shared/derive-independence.mjs`)

2026-08-22 re-vendor (I-2026-08-22-r46-vendored-reason-catalog, PR #15 upstream): closes
the R46 contract_boundary gap recorded in
docs/spec/I-2026-08-18-design-critic-injection/verification.yaml's test_gaps -- upstream
PR #15 turned every `reasons` string this module emits into a projection of a new,
additive `{code, params}` `reason_records` field (closed 20-value `REASON_CODES` set,
`formatReasonRecord` for the exact pre-existing wording). The previous pin
(d8309b47ed13d5f29182adbfd6e9eaeb723ca576, PR #7) predates this; `reasons`' wording is
byte-identical between the two commits (upstream's own PR #15 description: "全 15
review/84 reason 文字列の before/after スナップショット diff が空"), so this re-vendor is
additive-only from lane's side too -- see packages/core/src/design-independence.ts for how
the new `reason_records` are now consumed instead of the raw `reasons` strings.

Note on this pin's history: this file was first vendored against
6b4f4233cf07e79040d0b8b65d7fe658d497112d, a commit on the (then-unmerged) branch
`fix/independence-status-derivable`, because a stale local `origin/main` remote-tracking
ref made it look like the derivation and the engine_ref/critic/prior_involvement revision
of design-options/v1 did not exist on upstream `main` at all. `git fetch origin` in the
upstream checkout showed `main` had already moved past that (PR #7 merged as
5e8884e, with 6b4f423 as its last commit) -- the file contents at both commits are
byte-for-byte identical (`git diff 6b4f423:... 5e8884e:...` empty), so only this marker's
recorded commit changed, not the vendored bytes or their hash below. Re-pinning promptly
once a real re-check confirmed the correct upstream state, rather than leaving a
branch-limited commit pinned "temporarily," is exactly what
packages/adapters/test/fixtures/measure/UPSTREAM's own known defect failed to do.

derive-independence.d.mts in the same directory is NOT vendored -- it is a hand-written
TypeScript declaration lane authored so the .mjs can be imported from .ts sources without
`allowJs`. It is intentionally excluded from the tree hash below (re-vendoring the .mjs
does not require touching it unless the upstream module's exported shape itself changes).

Vendored file hash (sha256 over `find <dir> -name 'derive-independence.mjs' -type f | sort`
each file's path+content, reproducible via:
  find packages/core/src/vendor/derive-independence/v1 -name 'derive-independence.mjs' \
    -type f | sort | xargs -I{} sh -c 'echo "{}"; cat "{}"' | shasum -a 256
):

  e1bfe512d602e1e62e2cf736353417a4dd1a06e82b3fe6c46ade3fd6c7b51c1f

Re-vendoring: if the playbook's module changes, re-copy derive-independence.mjs from the
new commit, recompute the hash with the command above, and update both the commit id and
the hash in this file in the same commit as any code change that tracks the new revision.
**The pin MUST be a commit that is an ancestor of upstream `main`** -- see this marker's own
"Note on this pin's history" above and packages/adapters/test/fixtures/measure/UPSTREAM's
2026-08-20 re-pin history for two different ways a pin can silently stop being that.
packages/core/src/design-independence.ts records the semantic summary of how lane
consumes this module; this file's job is only to pin the exact bytes tested against.

Pin verification (R39/R40, packages/core/src/pin-verify.ts): the content-hash check above
runs unconditionally, always, everywhere (deterministic, no network). The SEPARATE check
that this pin still resolves and is an ancestor of upstream `main` requires a local
checkout of ai-agent-skills-playbook and is **mandatory in this repo's own CI**
(.github/workflows/ci.yml checks it out at `.upstream/ai-agent-skills-playbook` and sets
`LANE_UPSTREAM_PLAYBOOK_PATH` before running the test suite) -- this is the exact check
that would have caught packages/adapters/test/fixtures/measure/UPSTREAM's still-open
defect, had it existed then. It stays **opt-in** for anyone else running this test suite
(including an end user of the published npm package, who has no reason to have this
checkout): `LANE_UPSTREAM_PLAYBOOK_PATH` unset reports "unknown" (inconclusive), never a
false pass.

2026-08-23 upstream history rewrite: the playbook rewrote its entire published history
(git filter-repo --mailmap) to remove a corporate email from commit metadata; every commit
id changed (trees/blobs byte-identical, so the recorded content hash above is unchanged).
The "Vendored commit:" id above was re-pointed via the rewrite's old->new commit map
(archived at ~/oss-space/.email-rewrite-20260823/ai-agent-skills-playbook-commit-map.txt).
Short commit ids in older prose notes refer to pre-rewrite ids and resolve only via that map.
