node_modules/
dist/
*.tsbuildinfo
.DS_Store
*.tgz

# deterministic-discipline (dd) hook ledger, written by the dd Claude Code hooks registered locally
.dd/

# publish/spec-lane's package.json is hand-authored and tracked; everything else in that
# directory is generated by scripts/build-publish.mjs from the root README.md, root LICENSE,
# and packages/cli/resources -- committing copies would just be a second place for those to
# drift out of sync.
/publish/spec-lane/dist/
/publish/spec-lane/resources/
/publish/spec-lane/README.md
/publish/spec-lane/LICENSE
